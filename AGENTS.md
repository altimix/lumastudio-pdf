# LumaStudio PDF 開発ガイド

- 思考は英語で行い、ユーザー向けの出力・操作説明は日本語にする。
- Windows と macOS の Electron アプリ。元の PDF を保護し、署名済み PDF は閲覧専用を維持する。印影画像と証明書による電子署名を混同しない。
- API キー、`.env`、個人の PDF・印影・口座情報、P12/PFX・秘密鍵、`.lumapdf` をコミット・ログ出力・配布しない。
- 通常の編集は端末内で行う。文書の外部送信は利用者が AI を明示実行した場合のみ。通常テストは実 API を呼ばず、架空データを使う。
- 実装後は変更に応じて `npm test`、`npm run release:check`、`npm run build`、`npm run test:e2e` を実行し、実際の画面と保存結果を確認する。Electron の変更はパッケージの起動でも確認する。
- 原則は Issue に目的・完了条件を記録 → Issue 番号を含む作業ブランチ → Issue に紐づく PR → CI と `@codex review` → 指摘修正 → 最新コミットのチェック → squash merge。リリースはマージ後の成功済みコミットへのバージョンタグから行う。リリースを完了条件に含む Issue は配布物の検証後に閉じる。
- CI や自動テストで確認した範囲と、Mac の利用者環境・実印刷・本番証明書など未確認の範囲を分けて報告する。詳細は `docs/development.md`。
- この Windows 環境では制限付きの Electron 起動テストで子プロセスが異常終了した履歴がある。実アプリのスモークテストは承認された通常の実行環境で行い、同じ制限付き起動を繰り返さない。OS保護やElectron sandboxを無効化して回避しない。終了時はテスト用の隔離パスを確認したプロセスだけを整理する。
